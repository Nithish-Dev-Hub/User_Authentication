const Home = () => {
    return (
        <div>
            <h1>MERN Authentication</h1>
            <p>
                This is a boilerplate for MERN authentication that stores a JWT in
                an HTTP-Only cookie.
            </p>
        </div>
    );
};

export default Home;